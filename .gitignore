
# FVM Version Cache
.fvm/

# Flutter build output
build/

# Dart tool cache (local paths, package resolution)
.dart_tool/

# Flutter plugin dependency cache
.flutter-plugins-dependencies

# Platform-specific golden images (NOT committed)
test/golden/goldens/macos/
test/golden/goldens/linux/
test/golden/goldens/windows/